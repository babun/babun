#!/bin/bash
set -o pipefail
set -e
set -f

# does not overwrite files if they exist
# relative location - files as they reside in the exploded home!
declare -A skip
skip[".pact/pact.repo"]=true
skip[".babunrc"]=true
skip[".zshrc"]=true

